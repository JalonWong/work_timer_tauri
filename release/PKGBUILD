# Maintainer: Jalon Wong <jalonwong@gmail.com>
pkgname=Work-Timer
pkgver=1.0.0
pkgrel=1
pkgdesc="A Work Timer Application"
arch=('x86_64')
url="https://github.com/JalonWong/work_timer_tauri"
license=('Apache-2.0')

package() {
  install -Dm644 "${startdir}/../src-tauri/icons/icon.svg" \
    "${pkgdir}/usr/share/pixmaps/${pkgname}.svg"

  install -Dm644 "${startdir}/../src-tauri/target/release/_up_/static/sound.mp3" \
    "${pkgdir}/usr/lib/${pkgname}/_up_/static/sound.mp3"

  install -Dm755 "${startdir}/../src-tauri/target/release/bundle/deb/${pkgname}_${pkgver}_amd64/data/usr/bin/${pkgname}" \
    "${pkgdir}/usr/bin/${pkgname}"

  install -Dm644 "${startdir}/../src-tauri/target/release/bundle/deb/${pkgname}_${pkgver}_amd64/data/usr/share/applications/${pkgname}.desktop" \
    "${pkgdir}/usr/share/applications/${pkgname}.desktop"
}
